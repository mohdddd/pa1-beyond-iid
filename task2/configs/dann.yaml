# Task 2 Step 3: DANN — discriminator 512 -> 256 -> ReLU -> Dropout(0.5) -> 2, GRL.
run_name: dann
method:
  name: dann
  disc_hidden: 256
  disc_dropout: 0.5
  domain_loss_weight: 1.0
  grl_max: 1.0          # alpha(p) = grl_max * (2/(1+exp(-10p)) - 1), p = step / (30*203)
